{
  "extends": "next/core-web-vitals",
  "rules": {
    /* --- silence react-hooks warnings/errors --- */
    "react-hooks/exhaustive-deps": "off",
    "react-hooks/rules-of-hooks": "off",

    /* --- silence the @typescript-eslint missing/definition error and unused vars --- */
    "@typescript-eslint/no-unused-vars": "off",
    "no-unused-vars": "off"

    /* add any other global rule tweaks here */
  }
}
